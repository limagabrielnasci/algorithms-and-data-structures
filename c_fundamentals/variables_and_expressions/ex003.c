#include <stdio.h>

int main() {
    // Pec¸a ao usuario para digitar tr ´ es valores inteiros e imprima a soma deles.
    int n1, n2, n3;
    scanf("%d %d %d", &n1, &n2, &n3);
    int sum = n1 + n2 + n3;
    printf("sum = %d\n", sum);
}